18:// Helper function for crop fuzzy matching
41:  // Custom casing logic for presentation
51:  // Hirarki Posisi (dari yang tertinggi ke terendah):
52:  // Business Analyst (1) -> Sales Manager (2) -> Area Sales Manager (3) -> Sales Agronomist (4) -> Business Solution (5)
66:    // Prioritize explicit digit
69:    // Explicit roman numerals as fallback
91:    // Check if it's the logged-in user:
99:    // Check teamLevels state:
105:    // If teamProfiles exists directly on userData:
114:    // Fallback based on position name:
145:    // 1. Follow the direct upline path recursively to find the first ancestor who is in active teamMembers
163:    // 2. Strict mode: if no direct/transitive upline found in active teamMembers, return null (root)
175:        // Add subordinates from kiosks hierarchy
187:        // Add subordinates from employee sheet profile mapping
201:    // Also include 'listianto' under 'agusherdianto'
259:    // Find if there's a profile in teamProfiles that matches the rootName by name or by key or email
293:    // For any remaining nodes in teamProfiles, try to traverse up their upline to determine depth.
346:    // Also tag depths for the direct root alias so it functions correctly
504:            // silent frame error check
758:  // State Tab Home
770:  // State Tab Partner
779:  // State Tab Summary
785:  // State Tab POG
921:    // FALLBACK AMAN: Jika hasil filter tim sangat sedikit, atau user menggunakan akun demo/tidak terpetakan,
922:    // maka masukkan semua PIC yang ada di daftar Kiosks agar data Stock Summary/POG tetap tampil dan dapat dianalisis.
1191:      // 1. Lakukan Grouping (Gabungkan LOT & Hybrid yang sama)
1197:             // Ambil data dari timestamp terbaru (TIDAK ADA AKUMULASI QTY)
1206:      // 2. Filter dan format
1348:          // Jangan dihapus dari list, ubah saja condition-nya jadi 'habis'
1359:      // Ambil hanya list yang tampil (berdasarkan Kiosk yang terpilih)
1425:    // Kumpulkan channel unik yang memiliki aktivitas (timestamp) di bulan ini
1431:            // Parsing manual jika format dd/MM/yyyy HH:mm:ss
1537:  // Fungsi Parser Tanggal POG
1552:  // Kalkulasi & Filter Data POG
1571:      // Pre-compute maps to avoid nested array scans (O(N*M) -> O(N+M))
1672:      // Filter active POG team members based on selected crop filter
1767:          // Pre-compute uplines for activePogMembers to avoid O(M) recursive scans inside buildNode loops
1901:          // Drill down
2016:  // Ekstrak data list crops unik untuk filter
2028:  // Filter team members based on selected crop filter
2032:    // Check if any loaded employee record has group info to determine sheet/fallback mode
2071:      // Fallback mode: if group column info is empty or not in Google Sheet yet,
2072:      // filter members by their matching recorded crop in workingData so they always see correct data.
2073:      // A member is kept if they (or any of their subordinates recursively) have working data with the selected crop.
2103:      // Propagate active status upwards
2116:            // Match against actual teamMembers to handle partial names
2121:                // Already processed this branch upwards, can break early
2146:    // Pre-compute maps to optimize from O(N * M) to O(N + M)
2209:    // Filter Team AND Filter Crops (Using Set.has for O(1) performance)
2351:        // Sub-item
2482:            // ALWAYS return the teammate node (do not return null if 0 total) to support "tampilkan semua nama" from sheet employee
2490:        // Do not filter(Boolean) here as we want to preserve all employee roots.
2516:                // If sub-group is Team/Subordinate, ALWAYS show them even with 0 total
2524:            // If sub-group is Team/Subordinate, ALWAYS show the parent group even with 0 total
2562:    // Determine level badge style
2806:    // Determine level badge style
3148:                  // Dynamically resolve Layer 1 members:
3168:                          // Dynamically resolve Layer 2 members (whose upline is sub1)
3775:// Login Screen Component
3868:  // Load Google Material Symbols for icons
3877:    // Attempt to real login using Apps Script endpoint
